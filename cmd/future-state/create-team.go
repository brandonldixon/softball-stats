package cmd

// Future State to have different teams added
/*
import "fmt"

// A function to add a new team
// The function prompts for Team Name, Coach Name, and State, and generates a unique ID for the team
// This creates a new team struct for a dynamoDB table to be created and players added

func CreateTeam() *Team {
	t := Team{}
	var i, j, k string
	fmt.Println("Add a New Team to the Application")
	fmt.Println("Enter the Team Name")
	fmt.Scanln(&i)
	fmt.Println("Enter the Coach's Name")
	fmt.Scanln(&j)
	fmt.Println("Enter the Team's Home State")
	fmt.Scanln(&k)
	t.TeamName = i
	t.CoachName = j
	t.State = k
	t.TeamID = GenerateId()
	return &t
}
*/
